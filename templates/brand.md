# Brand guide
# This file is READ BY THE AI AS PROSE. It is NOT parsed by scripts/design.mjs --
# machine config (mascot, font, colors, output folder, backend) lives in show-me-how.md.
# Edit it once; /show-me-how:post reads it on every run. Keep the section headings.
# Comments start with #. Delete nothing you might want later -- comment it out.

## Positioning
one-liner: <what you make, for whom, in one sentence>
  # The single line every post should be able to ladder up to.
  # If a post idea cannot trace back to this line, it is off-brand.

## Audience
who: <who you are talking to; their role, their day, their pain>
  # Write to one person from this group, never to everyone.
  # e.g. "solo founders shipping dev tools, allergic to marketing fluff"

## Voice & tone
voice: <3-5 adjectives, e.g. direct, warm, a little odd, never salesy>
examples:
  # 2-3 lines that sound like you. The AI imitates these more than the adjectives.
  # - "Ship the boring version first."
  # - "Your backlog is a graveyard of good intentions."
  - <example line 1>
  - <example line 2>
do: <what the voice always does, e.g. plain words, short sentences, one idea per post>
don't: <what it never does, e.g. hustle-bro hype, buzzwords, exclamation marks>

## Offerings
  # What you sell or give away, one per line: name + one-line promise.
  # Posts point at these; there is nothing else to sell.
  # - Widget: "the linter that explains itself"
- <offering 1>

## Proof points
  # Numbers, names, results that make claims believable, one per line.
  # - "2,400 teams run it in CI"
- <proof 1>

## Calls to action
primary: <the one thing a reader should do, with its URL, e.g. "Try the demo -- https://...">
secondary: <the softer ask, e.g. "Follow for weekly notes">
  # Every post carries exactly one CTA, usually the primary.

## Brand hashtags
  # 3-8 tags you always rotate through; the post skill mixes these with topic tags.
  # - #yourbrand #buildinpublic
- <tag 1>

## Platform handles
instagram: <@handle>
linkedin: <page or profile URL>

## Visual style
look: <colors, fonts, imagery direction in prose>
  # e.g. "paper-white backgrounds, black hand-drawn lines, one orange accent;
  # the mascot does the work; never stock photos"
style references:
  # 1-3 image paths in this repo used as style anchors, passed to the image
  # backend with --ref, one per line, e.g.
  # - brand/campaign-01.png
  # Place the image files in the repo yourself; missing paths are dropped.

## Avoid
  # Content and visuals the brand never touches, one per line.
  # - competitor names, politics, engagement bait, corporate flat art
- <avoid 1>

## Platform notes
  # Anything platform-specific: cadence, link policy, pinned-comment habit.
instagram: <notes, or "none">
linkedin: <notes, or "none">
